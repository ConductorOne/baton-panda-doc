name: ci
on:
  pull_request:
    types: [opened, reopened, synchronize]
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    environment: test
    env:
      BATON_LOG_LEVEL: debug
      BATON_ENVIRONMENT: "test"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: Build baton-panda-doc
        run: go build ./cmd/baton-panda-doc
      - name: Run baton-panda-doc
        run: ./baton-panda-doc --api-key=${{ secrets.BATON_API_KEY }}
